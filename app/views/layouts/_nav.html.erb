
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/home">Classified Hero</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/home">Home <span class="sr-only">(current)</span></a>
      </li>
     
      <li class="nav-item">
        <a class="nav-link" href="/articles">Posts</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/about">About</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/users">Members</a>
      </li>
    </ul>
    <div class="collapse navbar-collapse mr-4" id="navbarText">
    
      <ul class="navbar-nav ml-auto">
         <% if logged_in? && current_user.admin? %>
            <button type="button" class="btn btn-outline-secondary disabled">Admin</button>
         <%end%>
      
        <div class="btn-group">
          <button type="button" class="btn btn-outline-info disabled">  <%= logged_in? ? current_user.username : "Welcome" %></button>
          <button type="button" class="btn btn-outline-info dropdown-toggle dropdown-toggle-split mr-5" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <span class="sr-only">Toggle Dropdown</span>
          </button>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">  
    <% if logged_in? %>
      <%= link_to "My Profile", user_path(current_user), method: :get, class: "text-white dropdown-item" %>
      <%= link_to "Delete Profile", user_path(current_user), method: :delete, class: "text-danger dropdown-item",data: {confirm: "Are you sure you want to permanently delete?"} %>
      <%= link_to "Logout", logout_path, method: :delete, class: "dropdown-item text-white" %>
      <% else %>
      <%= link_to "Sign Up", signup_path, class:"dropdown-item text-white" %>
      <%= link_to "Login", login_path, class:"dropdown-item text-white" %>
      <% end %>
  </div>
</div>
      </ul>
    </div><!--/.nav-collapse -->

  </div>
</nav>